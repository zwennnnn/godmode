---
project_name: {name}
slug: {slug}
created_at: {date}
target_market: {market}
---

# Build Plan — {name}

> Auto-generated by `/godhunt` on {date}. Generated by godmode's scoring engine; no human input.

## Phases

### Phase 0 — Bootstrap (Day 1)
- [ ] `mkdir -p projects/{slug}` ✅ (already created by `/godhunt`)
- [ ] Run `git init`
- [ ] Initialize the tech stack scaffold: `{frontend}` + `{backend}` (see [README.md](README.md))
- [ ] Set up `.env` from `.env.example` (Minimax API key + secrets)

### Phase 1 — Core data model (Day 2–3)
- [ ] Define entities / DB schema (per MVP scope in README.md)
- [ ] Set up Drizzle / Prisma / SQLAlchemy migrations
- [ ] Seed sample data

### Phase 2 — AI integration (Day 4–6)
- [ ] Wire Minimax API client (per `tech-stack` in README.md)
- [ ] Build prompt templates per feature
- [ ] Add streaming responses
- [ ] Cache + rate-limit

### Phase 3 — UI (Day 7–10)
- [ ] Implement core screens per MVP scope
- [ ] Auth flow (per stack choice)
- [ ] Responsive + a11y
- [ ] Error states + loading skeletons

### Phase 4 — Integration (Day 11–12)
- [ ] End-to-end test of each feature
- [ ] Connect any third-party APIs
- [ ] Webhooks (if applicable)

### Phase 5 — Deploy (Day 13–14)
- [ ] Provision infra (per `{deploy}` in README)
- [ ] Set up CI
- [ ] Production env (secrets, domains)
- [ ] Smoke test + monitoring

### Phase 6 — Launch prep
- [ ] Landing page copy
- [ ] Analytics (PostHog / Plausible)
- [ ] First-user onboarding

## Acceptance criteria

- [ ] All MVP features (per README.md) work end-to-end
- [ ] Minimax API integration is the only LLM dependency
- [ ] Production build under 2 MB JS bundle (if web)
- [ ] A11y WCAG 2.2 AA on key screens
- [ ] Lighthouse perf ≥ 90

## Cost estimate (MVP)

| Item | Estimate |
|------|----------|
| Minimax API (10K requests/mo @ typical) | ~$20/mo |
| Hosting (Vercel/Fly free tier → Pro) | $0–25/mo |
| DB (Postgres/Neon free → paid) | $0–19/mo |
| Domain | $12/yr |
| **Total MVP** | **~$50/mo** |

## Decision log

- {date}: Auto-generated by `/godhunt` — chosen for {market} market fit + AI-API-only feasibility.
- {date}: Tech stack scored via `python scripts/score.py --stage {stage}`.

## Next steps

1. Confirm tech stack in [README.md](README.md).
2. Run `git init && git add . && git commit -m "Initial scaffold from /godhunt"`.
3. Begin Phase 0 manually — `/godhunt` doesn't ship code, only the plan + scaffold.
